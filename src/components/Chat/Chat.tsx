import ChatWindow from "../ChatWindow/ChatWindow";
import ChatSidebar from "../ChatSidebar/ChatSidebar";

const Chat = () => {
  return (
    <div>
      <ChatSidebar />
      <ChatWindow />
    </div>
  );
};

export default Chat;
